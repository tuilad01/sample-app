import { FilterWordByGroupIdPipe } from './filter-word-by-group-id.pipe';

describe('FilterWordByGroupIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWordByGroupIdPipe();
    expect(pipe).toBeTruthy();
  });
});
